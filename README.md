# Smart Hosting Website — V5 Fixed

This version fixes the responsive header/logo issue and includes the required image folders.

## Files
- HTML pages in the repository root
- `css/style.css` — shared styling and responsive mobile navigation
- `assets/smart-hosting-icon.png` — Smart Hosting house + key + temperature icon used in the header
- `favicon.png` — browser tab icon

## GitHub upload
Upload the **contents** of this folder to the root of the `achatte/smartlyhosted` repository. Do not create an extra `SmartHostingWebsite_v5_fixed` folder in the repository.

The signup form remains a front-end demo and should be connected to a real consent-recording service before using it as the final 10DLC web opt-in mechanism.
